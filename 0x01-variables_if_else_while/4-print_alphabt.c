#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: program prints the all alphabet apart 
 * fro q and e in lower case 
 *
 * Return: Always 0 (Success)
 *
 */
int main (void)
{
	char ch;

	for (ch= 'a'; ch<= 'z'; ch++)
	{
		char *ch = ch+5;
		char *ch = ch+17;
		putchar(ch);
	}

