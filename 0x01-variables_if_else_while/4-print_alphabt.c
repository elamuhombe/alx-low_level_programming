#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: program prints the all alphabet apart 
 * fro q and e in lower case 
 *
 * Return: Always 0 (Success)
 *
 */
int main (void)
{
	char ch= [a-z];
	char *e= ch+1;
	putchar(e);
	return (0);

