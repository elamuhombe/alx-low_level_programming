#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints all lowercase letters
 *
 * Return: Always 0 (Success)
 */
int main (void)
{
	char ch

		for (ch = 'a'; c <= 'z'; ch++)
		{
			putchar(ch"\n");
		}
	return (0);
}
