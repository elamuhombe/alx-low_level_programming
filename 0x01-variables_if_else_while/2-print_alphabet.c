#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints all lowercase letters
 *
 * Return: Always 0 (Success)
 */
int main (void)
{
	char c

		for (c = 'a'; c <= 'z'; c++)
		{
			putchar(c"\n");
		}
	return (0);
}
